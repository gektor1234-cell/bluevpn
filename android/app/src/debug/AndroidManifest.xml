<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The INTERNET permission is required for development. Specifically,
         the Flutter tool needs it to communicate with the running application
         to allow setting breakpoints, to provide hot reload, etc.
    -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <queries>
        <package android:name="pro.greenvpn.transportprobe" />
        <package android:name="pro.greenvpn.transportprobe.direct" />
    </queries>
    <application>
        <activity
            android:name="pro.greenvpn.app.VpnConsentDebugActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:permission="android.permission.DUMP"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />
        <service
            android:name="pro.greenvpn.app.TransportContractDebugService"
            android:permission="android.permission.DUMP"
            android:exported="true" />
    </application>
</manifest>
